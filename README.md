# landscape-gacha

日本の地形ガチャ (MVP)。
DuckDB WASM + Spatial を使って、国土地理院の基準点(cp) GeoJSONタイルからランダムに1件を表示します。

## Development

```sh
pnpm install
pnpm dev
```

## Build

```sh
pnpm run build
pnpm run preview
```

## Check

```sh
pnpm run check
```
